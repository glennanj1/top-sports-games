RSpec.describe Top::Sports::Games do
  it "has a version number" do
    expect(Top::Sports::Games::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
